monolog:
    channels: ['payment']
    handlers:
        payment_logger:
            level:    debug
            type:     stream
            path:     '%kernel.logs_dir%/payment.%kernel.environment%.log'
            channels: [payment]
